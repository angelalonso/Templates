FROM ubuntu:22.04

RUN apt-get update && apt-get install -y \
    build-essential \
    gcc-mingw-w64 \
    gcc-multilib \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY . .

RUN chmod +x scripts/cross-compile.sh
CMD ["./scripts/cross-compile.sh"]
